/* Manifest line exceeds 4096-byte fgets buffer.
 * phc must either handle it correctly or error explicitly.
 * Silent truncation is a bug — it defeats exhaustive matching. */
int main(void) { return 0; }
