phc_descr Shape {
    Circle { double radius; },
    Rectangle { double width; double height; }
};

void f(Shape s) {
    phc_match(Shape, s) {
        case Circle(nonexistent): {
        } break;
        case Rectangle: {
        } break;
    }
}
