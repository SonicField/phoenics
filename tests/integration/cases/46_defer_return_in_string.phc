#include <stdio.h>

int main(void) {
    phc_defer { printf("cleanup\n"); }

    /* return inside string must NOT be treated as a return statement */
    printf("return 0;\n");
    printf("fake return\n");
    return 0;
}
