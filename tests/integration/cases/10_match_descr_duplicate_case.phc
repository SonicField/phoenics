phc_descr AB {
    A { int x; },
    B { int y; }
};

int main(void) {
    AB v = AB_mk_A(1);
    phc_match(AB, v) {
        case A: { break; }
        case B: { break; }
        case A: { break; }
    }
    return 0;
}
