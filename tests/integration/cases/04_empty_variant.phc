#include <stdio.h>

descr Result {
    Ok { int value; },
    Err { int code; const char *message; }
};

int main(void) {
    Result r = Result_mk_Ok(42);
    match_descr(Result, r) {
        case Ok: {
            printf("ok: %d\n", r.Ok.value);
        } break;
        case Err: {
            printf("err %d: %s\n", r.Err.code, r.Err.message);
        } break;
    }
    return 0;
}
